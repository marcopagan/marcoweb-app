export type ColorScheme = {
    themeName: string,
    isDark: boolean,
    fg: string,
    bg: string,
    liveborder: string,
    live: string,
    permaborder: string,
    perma:string,
    invalid: string
}